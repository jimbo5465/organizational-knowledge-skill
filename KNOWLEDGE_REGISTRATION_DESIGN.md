# ثبت دانش فنی در WelderBot — سند طراحی

| | |
|---|---|
| **سند** | KNOWLEDGE_REGISTRATION_DESIGN.md |
| **پروژه** | WelderBot (مدیریت WQT — ASME Section IX) |
| **نگارش** | Revision 0 |
| **تاریخ** | 2026-08-11 |
| **وضعیت** | پیشنویس — برای بحث و تأیید |
| **پایپلاین دانش مرجع** | مهارت `organizational-knowledge-skill` v2.0.1 (Hermes) |

## تاریخچهٔ نگارش

| Rev | تاریخ | تغییرات | نویسنده |
|---|---|---|---|
| 0 | 2026-08-11 | نگارش اولیه — تصمیمات معماری ثبت دانش | Mohsen + Hermes |

---

## 1. زمینه و هدف

WelderBot یک بات تلگرامی مدیریت آزمونهای صلاحیت جوشکاری (WQT) بر مبنای ASME Section IX است. این سند چگونگی استفاده از WelderBot را برای **ثبت دانش فنی سازمانی** (تجربهها، پیشنهادها، دانش صریح) در قالب پایپلاین DANA تعریف میکند.

هدف: اپراتور/مهندس بتواند دانشی که هنگام کار به دست میآید را بهسادگی از داخل تلگرام ثبت کند، و خروجی نهایی یک **پیشنویس ساختاریافته و آمادهٔ ثبت در DANA** باشد — بدون اینکه هوش مصنوعی جای تصمیم انسانی را بگیرد.

---

## 2. تصمیمهای کلیدی (خلاصه)

| # | تصمیم | منطق |
|---|---|---|
| K1 | **NCR از ثبت تجربه/پیشنهاد جدا میشود** و مسیر فرمی خودش را حفظ میکند | NCR یک فرم سازمانی با چرخهٔ حیات، شمارهگذاری و خروجی اکسل مستقل است؛ قاطی کردنش با ثبت دانش هر دو را خراب میکند |
| K2 | **یک کلید واحد «ثبت دانش»** برای تجربه/پیشنهاد/دانش صریح | طبقهبندی کار موتور هوش است، نه کار دکمه |
| K3 | **دکمه = نیت کاربر، نه تصمیم طبقهبندی** | انتخاب کاربر بهعنوان `User Intended Type` در CKO ثبت میشود؛ موتور همیشه تشخیص معنایی میدهد و در صورت اختلاف، مکانیزم تعارض فعال میشود |
| K4 | **راهنمای ورود، واقعیت جمع میکند، نه برچسب** | راهنما نمیگوید «تجربهست یا پیشنهاد؟»؛ میگوید «چی شد، چیکار کردی، نتیجه چی بود؟» — تا bias ورودی ایجاد نشود |
| K5 | **تأیید نهایی همیشه با انسان است** | مهارت مرجع: «موتورها پیشنویس میسازند، اپراتور ثبت میکند» |

---

## 3. معماری کلی

```
┌─────────────────────────────────────────────────────────────┐
│                       WelderBot (تلگرام)                     │
│                                                             │
│  ⚠️ فرم NCR (موجود)           📚 ثبت دانش (جدید، یکپارچه)    │
│  draft → submitted            ویزارد: واقعیتها → پیشنویس    │
│        │                            │                       │
│        │ (منبع دانش بعدی،            │                       │
│        │  تبدیل دستهای)              ▼                       │
│        └──────────┬─────────────────┘                       │
└───────────────────┼─────────────────────────────────────────┘
                    ▼
     ┌──────────────────────────────────┐
     │   پایپلاین دانش (organizational- │
     │   knowledge-skill — ۷ موتور)      │
     │   طبقهبندی → محتوا → متادیتا → QA │
     │   → پیشنویس DANA                  │
     └──────────────────────────────────┘
                    ▼
          بازبینی انسانی (اپراتور/مدیر)
                    ▼
                ثبت در DANA
```

**اصل کلیدی:** جداسازیِ *مسیر ورود* ≠ جداسازیِ *جریان دانش*. NCR مسیر فرمی خودش را میرود، اما بعداً همان NCRهای بستهشده میتوانند خوراک دستهای پایپلاین دانش شوند (تبدیل به Lesson Learned) — در غیر این صورت تجربههای داخل NCRها برای همیشه دفن میشوند.

---

## 4. مسیر NCR (جدا — وضعیت موجود)

- فرم ساختاریافتهٔ موجود با چرخهٔ حیات `draft → submitted` حفظ میشود.
- فیلدهای فعلی: `ncr_number, project_id, contractor_id, island, unit, operation_type, discipline, drawing_number, description, cause, corrective_action, hse_confirmed, equipment_description, reporter_name/title, reported_by/date, photos`.
- **محدودیت شناختهشده برای استفادهٔ دانشی:** NCR دارای `description → cause → corrective_action` است (متناظر با Context → Problem → Action در مدل Lesson Learned) ولی فیلد **Result و Lesson را ندارد**. برای تبدیل به دانش، قدم پایانی باید تکمیل شود (پرسش از اپراتور یا تکمیل در زمان بستن NCR).
- **فاز بعدی (خارج از این Rev):** تبدیل دستهای NCRهای بستهشده به پیشنویس Lesson Learned با نگاشت:
  - `description` → Problem / Situation
  - `cause` → Cause / Contributing Factors
  - `corrective_action` → Action
  - (گمشده: Actual Result, Lesson, Transferability — نیاز به تکمیل)

---

## 5. مسیر «ثبت دانش» — ویزارد یکپارچه

### 5.1 ساختار ویزارد (جمعآوری واقعیت)

```
📚 ثبت دانش
┌────────────────────────────────────────┐
│ 1. عنوان کوتاه موضوع                     │
│    (مثال: مشکل ترک در جوش ریشه لوله)     │
│ 2. شرح کامل — فقط واقعیتها:             │
│    چی شد / چیکار کردی / نتیجه چی بود     │
│ 3. آیا این کار تا الان اجرا شده؟         │
│    [بله] [خیر] [در حال انجام]           │
│ 4. نتیجهٔ واقعی / نتیجهٔ مورد انتظار     │
│ 5. عکس یا فایل: (اختیاری)               │
│ 6. پروژه: (از لیست پروژههای بات)        │
└────────────────────────────────────────┘
```

### 5.2 قوانین راهنما (الزامی)

1. راهنما **واقعیت** میخواهد، نه **برچسب** — جملهبندی راهنما نباید اپراتور را به قالب «تجربه» یا «پیشنهاد» هدایت کند.
2. تنها سؤال «نیمه-واقعی» مجاز در ویزارد: **«آیا این کار اجرا شده؟»** — چون جواب (بله/خیر/در حال انجام) یک واقعیت است و همان نقطهٔ تمایز تجربه از پیشنهاد است.
3. سؤالهای دستهبندی («این تجربهست یا پیشنهاد؟») **ممنوع** — این تصمیم متعلق به موتور طبقهبندی است.
4. فیلد ۶ (پروژه) از دادههای موجود WelderBot پیشفرض میشود — ورود دوبارهٔ دستی لازم نیست.
5. اصل کمینهگرایی (Minimum Clarification): فقط فیلدهای لازم پرسیده شود.

### 5.3 خروجی ویزارد → CKO

خروجی ویزارد مستقیماً به ساختار Common Knowledge Object نگاشت میشود:

| فیلد ویزارد | فیلد CKO |
|---|---|
| عنوان | `title` |
| شرح | `description` / `context` / `problem` |
| اجرا شده؟ | `implementation_status` (برای پیشنهاد) / `action` (برای تجربه) |
| نتیجه | `actual_result` / `expected_impact` |
| عکس/فایل | `attachments` / `primary_resource` |
| پروژه | `project` (operator-declared — بدون تغییر حفظ شود) |
| انتخاب دکمه | `user_intended_type` (فقط بهعنوان context) |

---

## 6. طبقهبندی و حل تعارض

1. موتور Knowledge Classification بر اساس **معنای محتوا** طبقهبندی میکند: Lesson Learned / Suggestion / Explicit Knowledge (با ۹ زیرنوع).
2. انتخاب دکمهٔ کاربر بهعنوان `User Intended Type` ثبت میشود — **هرگز بهتنهایی تعیینکننده نیست**.
3. در صورت اختلاف (کاربر «پیشنهاد» زده ولی محتوا تجربهٔ اجراشده با نتیجه است):
   ```
   🤖 طبقهبندی: این یک «تجربه» است
      دلیل: رویداد واقعی + اقدام اجراشده + نتیجهٔ گزارششده
   [تأیید] [اشتباهه — پیشنهاده] [ویرایش]
   ```
4. اپراتور میتواند پس از دیدن استدلال، طبقهبندی را override کند — این حق انسانی است و باید در رابط کاربری صریح باشد.
5. در موارد مبهم، موتور **یک سؤال شفافسازی هدفمند** میپرسد (مثال: «این روش اجرا شده و نتیجه داشته، یا برای آینده پیشنهاد میکنی؟») — نه سؤالهای عمومی.

---

## 7. مدل دادهٔ پیشنهادی (جدول جدید)

در دیتابیس SQLite موجود WelderBot:

```sql
CREATE TABLE IF NOT EXISTS knowledge_entries (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    submitted_by    INTEGER NOT NULL REFERENCES users(id),
    project_id      INTEGER REFERENCES projects(id),
    contractor_id   INTEGER REFERENCES contractors(id),
    user_intended_type TEXT,               -- انتخاب دکمه (context فقط)
    title           TEXT,
    raw_text        TEXT,                  -- شرح واقعی اپراتور
    attachments     TEXT,                  -- JSON: مسیر فایلها
    cko             TEXT,                  -- JSON: Common Knowledge Object
    engine_type     TEXT,                  -- خروجی طبقهبندی موتور
    qa_status       TEXT,                  -- PASS / REVIEW_REQUIRED / FAIL
    dana_draft      TEXT,                  -- پیشنویس نهایی
    status          TEXT NOT NULL DEFAULT 'draft'
                    CHECK (status IN ('draft','classified','reviewed','submitted','rejected')),
    created_at      TEXT NOT NULL,
    updated_at      TEXT
);
CREATE INDEX IF NOT EXISTS idx_knowledge_status ON knowledge_entries(status);
```

---

## 8. خط قرمزها (غیرقابل مذاکره)

1. **هیچگاه ثبت خودکار به DANA** — خروجی همیشه پیشنویس است؛ ثبت نهایی با اپراتور است (Human Review Boundary).
2. **عدم خلط دو مفهوم Access Level:**
   - WelderBot: سطوح دسترسی (ادمین/مدیر پیمانکار/اپراتور) ← کنترل *دسترسی به بات*
   - DANA: Normal / Confidential / Highly Confidential ← محرمانگی *سند*
3. **Generalization Control** — دانش یک پروژه/پیمانکار خودبهخود قانون سازمانی نمیشود؛ دامنهٔ کاربرد باید صریح حفظ شود.
4. **Non-fabrication** — هیچ موتوری حق ساخت عدد، تاریخ، نتیجه یا ادعای پشتیبانینشده ندارد؛ `UNKNOWN` بر پاسخ اختراعی برتری دارد.
5. **راهنما برچسب نمیسازد** — جملهبندی ویزارد نباید اپراتور را به سمت قالب خاصی هدایت کند.

---

## 9. نکات باز (Open Questions — تصمیم در Rev بعدی)

| # | سؤال | گزینهها | پیشنهاد فعلی |
|---|---|---|---|
| Q1 | ورود سند/لینک (دانش صریح) کجا برود؟ | (الف) شاخهٔ همان ویزارد «ثبت دانش» (ب) دکمهٔ جدا | (الف) — چون «سند» یک فرم است، نه یک نوع دانش |
| Q2 | تأیید چندمرحلهای؟ | (الف) فقط اپراتور (ب) اپراتور + مدیر دانش | بستگی به ساختار سازمانی — پیشنهاد (ب) |
| Q3 | هوش کجا اجرا شود؟ | (الف) بلادرنگ در خود بات (۹router) (ب) صف پردازش بعدی | (الف) با الگوی ai_helper اثباتشده |
| Q4 | تبدیل دستهای NCR به دانش | همزمان با Rev 0 یا فاز بعدی؟ | فاز بعدی |
| Q5 | انتخاب گره Knowledge Tree | انتخاب آزاد یا محدود به چند گرهٔ مرتبط با جوش/QC؟ | محدود — با `references/knowledge-tree.md` |

---

## 10. ملاحظات فنی (VPS)

- **RAM:** اتصال به 9router از طریق HTTP است — تقریباً صفر افزایش مصرف RAM. جدول جدید + چند هندلر سبک.
- **هزینهٔ AI:** طبقهبندی و ساختاردهی با مدل رایگان `deepseek-v4-flash-free` از طریق 9router. پردازش انبوه تاریخی → دستهای (batch)، نه رکورد-به-رکورد.
- **مدل استقرار:** فرایندهای دائمی باید تحت systemd با `Restart=always` باشند — طبق الگوی موجود `welderbot.service`.

---

## 11. مرجع

- مهارت: `organizational-knowledge-skill` v2.0.1 — مسیر: `~/.hermes/skills/knowledge-management/organizational-knowledge-skill/`
- فایلهای کلیدی مهارت:
  - `SKILL.md` — ارکستراتور (ترتیب اجرای موتورها)
  - `references/knowledge-classification.md` — طبقهبندی معنایی
  - `references/lesson-learned.md` / `suggestion.md` / `explicit-knowledge.md` — موتورهای محتوا
  - `references/metadata.md` — نگاشت متادیتا
  - `references/quality-assurance.md` — گیت کیفیت
  - `references/dana-draft.md` — پیشنویس نهایی
  - `references/knowledge-tree.md` — درخت دانش رسمی (MAPNA)
- سندهای پروژه: `ARCHITECTURE.md`، `CONTRACTS.md`، `RUN.md`

---

*پایان Revision 0*
